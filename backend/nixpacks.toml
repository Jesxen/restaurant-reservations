# Railway / Nixpacks build for the Laravel API.
# Nixpacks auto-detects PHP from composer.json and runs `composer install`.
# We only override the start command: run migrations, then serve on $PORT.
[start]
cmd = "php artisan migrate --force --seed && php artisan serve --host=0.0.0.0 --port=${PORT:-8000}"
