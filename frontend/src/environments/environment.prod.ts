// Production config. `ng build` (defaultConfiguration: production) swaps
// environment.ts for this file via fileReplacements in angular.json.
// Set apiUrl to your deployed backend, e.g. https://restaurante-api.up.railway.app/api
export const environment = {
  production: true,
  apiUrl: 'https://REPLACE-WITH-BACKEND-URL/api',
};
